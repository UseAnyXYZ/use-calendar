{
  "name": "@useanysh/calendar-cli",
  "version": "0.4.1",
  "type": "module",
  "description": "CLI for use-calendar",
  "bin": {
    "use-calendar": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@useanysh/calendar-contracts": "workspace:*",
    "qrcode-terminal": "^0.12.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/qrcode-terminal": "^0.12.0",
    "typescript": "^5.8.0",
    "vitest": "^3.1.0"
  }
}
