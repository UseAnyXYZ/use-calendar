---
"@useanysh/calendar-cli": minor
---

Add reminder support with `--reminder <minutes>` flag for `events create` and `events update` commands. Multiple reminders can be specified by repeating the flag. Reminders are displayed in event output (e.g. "1h 30m before").
